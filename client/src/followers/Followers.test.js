import React from 'react';
import {render} from '@testing-library/react';
import {when} from 'jest-when';

describe('The followers component', () => {
    test('should render with three user names', async () => {
        expect(true).toBe(true);
    });
});